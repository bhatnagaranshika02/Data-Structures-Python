def insertion(l):
    for i in range(1,len(l)):
        key=a[i]
        j=i-1
        while j>=0 and key<arr[j]:
             arr[j+1] = arr[j]
             j-=1
        arr[j+1] = key
l=list(map(int,input().split()))
insertion(l)
print(l)