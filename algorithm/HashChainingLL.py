class Node:
	def __init__ (self,data):
		self.data =data
		self.next = None

class linkedlist:
	def __init__ (self):
		self.head = None

def hashing(keyValue):
	return keyValue%len(hashtable)

def insert(hashtable,keyValue,value):
	hash_key = hashing(keyValue)
    hashtable[hash_key].append(value)
	newnode=Node(hashtable)
	if self.head==None:
		self.head = newnode

	else:
		temp = self.head
		while temp!=None:
			temp=temp.next

		temp = newnode
		temp.next = None

def display()
			



	